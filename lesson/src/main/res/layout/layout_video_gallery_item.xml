<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"

    >

    <data>

        <variable
            name="videoData"
            type="com.shuange.lesson.modules.video.bean.VideoData" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">


        <RelativeLayout
            android:id="@+id/videoContainer"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:background="@color/hex_3AD08F"
            app:layout_constraintBottom_toTopOf="@+id/textTv"
            app:layout_constraintTop_toTopOf="parent">

            <com.google.android.exoplayer2.ui.StyledPlayerView
                android:id="@+id/video"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:layout_centerVertical="true" />
        </RelativeLayout>

        <ImageView
            android:id="@+id/heartsIv"
            android:layout_width="23dp"
            android:layout_height="23dp"
            android:scaleType="fitXY"
            android:src="@drawable/selector_heart"
            app:layout_constraintBottom_toTopOf="@+id/heartsTv"
            app:layout_constraintEnd_toEndOf="@+id/heartsTv" />

        <TextView
            android:id="@+id/heartsTv"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginEnd="16dp"
            android:layout_marginBottom="12dp"
            android:text="@{videoData.galleryItem.heartText}"
            android:textColor="@color/white"
            android:textSize="12dp"
            app:layout_constraintBottom_toBottomOf="@+id/videoContainer"
            app:layout_constraintEnd_toEndOf="parent"
            tools:text="23W" />

        <TextView
            android:id="@+id/textTv"
            android:layout_width="match_parent"
            android:layout_height="106dp"
            android:background="@color/black"
            android:paddingStart="20dp"
            android:paddingTop="12dp"
            android:paddingEnd="20dp"
            android:text="@{videoData.text}"
            android:textColor="@color/white"
            android:textSize="18dp"
            app:layout_constraintBottom_toBottomOf="parent"
            tools:text="Do you know what's going to happen?" />


    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>